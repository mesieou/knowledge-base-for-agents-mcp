# Canonical image to deploy.
# Default is a local image built from this repo checkout.
# For registry deploys, use an immutable tag like:
# ghcr.io/mesieou/knowledge-base-for-agents-mcp:sha-<git-sha>
MCP_IMAGE=knowledge-base-mcp:local
MCP_BUILD_LOCAL=1

# Compose/runtime settings
COMPOSE_PROJECT_NAME=knowledge-base-mcp
MCP_CONTAINER_NAME=knowledge-base-mcp
MCP_BIND_HOST=0.0.0.0
MCP_PUBLISHED_PORT=8000
MCP_ENV_FILE=.env.deploy
MCP_RUNTIME_ENV_FILE=.env.runtime
